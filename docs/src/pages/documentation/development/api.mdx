# REST API

monetr is built with an "API first" design. As such all of the functionality that monetr offers is available via the
REST API.

The API and the documentation around it is still changing rapidly, so integrating with monetr's API may be error prone
as newer versions are released.

